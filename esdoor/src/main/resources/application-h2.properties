# H2 in-memory DB
spring.datasource.url=jdbc:h2:mem:esdoordb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Flyway devre disi (H2 ile migration ugrasmayalim)
spring.flyway.enabled=false

# H2 web console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console


# IntelliJ Run/Edit Configurations ? Program arguments:
#--spring.profiles.active=h2
#
# for running jar
#java -jar target\esdoor-0.0.1-SNAPSHOT.jar --spring.profiles.active=h2
